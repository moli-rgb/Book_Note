<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Your Library</title>
  <link rel="stylesheet" href="/book.css">
  <link rel="stylesheet" href="/aos/aos.css" />
</head>
<body>
    <%- include("header") %>
  <h1>Your Books</h1>

<div class="cards">
  <% books.forEach(book => { %>
    <div style="display: inline-block; margin-bottom: 20px;">
      <div class="card"
      data-aos="zoom-in" data-aos-duration="1500">
        <img src="<%= book.img_url %>" alt="book cover">
        <h3><%= book.title %></h3>
        <p><strong>Author:</strong> <%= book.author %></p>
        <p><strong>Rating:</strong> 
          <% for (let i = 1; i <= 5; i++) { %>
            <% if (i <= book.rating) { %> ⭐ <% } else { %> ☆ <% } %>
          <% } %>
        </p>
        <p><strong>Note:</strong> <%= book.note %></p>
      </div>

      <!-- الأزرار بتاعة الكتاب ده (بره الكارد بس تحته على طول) -->
      <div class="book-actions" style="display: flex; justify-content: center; gap: 15px; margin-top: 10px; width: 100%;">
        <a href="/edit/<%= book.id %>">
          <button class="edit-btn">✏️ Edit</button>
        </a>

        <form action="/delete/<%= book.id %>" method="POST">
          <button type="submit" class="delete-btn">❌ Delete</button>
        </form>
      </div>
    </div>
  <% }) %>
</div>
 <script src="/aos/aos.js"></script>
  <script>AOS.init();</script>
  <%- include("footer") %>
</body>
</html>